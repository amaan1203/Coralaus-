# Core
streamlit>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0

# PDF Parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0
grobid-tei-xml>=0.1.0
PyPDF2>=3.0.0
requests>=2.31.0
rapidfuzz>=3.0.0
sentence-transformers>=2.2.0   # repo_validator: embedding similarity for title matching (optional but recommended)

# LLM Providers
google-generativeai>=0.3.0
groq>=0.4.0

# Semantic Scholar API (replaces dead PapersWithCode MCP)
# No extra package needed — uses requests (already listed above)
# Optional: set SEMANTIC_SCHOLAR_API_KEY in .env for 10 req/s rate limit
